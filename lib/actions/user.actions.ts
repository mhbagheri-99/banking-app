"use server";

export const signIn = async (email: string, password: string) => {
  try {
  } catch (error) {
    console.error(error);
  }
};

export const signUp = async (data: SignUpParams) => {
  try {
  } catch (error) {
    console.error(error);
  }
};
